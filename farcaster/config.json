export const getFrameConfig = (nftData = {}) => ({
    version: "vNext",
    imageUrl: nftData.image || "https://i.imgur.com/muoYEIu.jpeg",
    postUrl: window.location.href,
    buttons: [
        {
            label: nftData.buttonLabel || "🪐 Mint NFT",
            action: "post"
        }
    ],
    input: {
        text: "Enter wallet address (0x...)"
    }
});